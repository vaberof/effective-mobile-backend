app:
  http:
    server:
      host: localhost
      port: 8000

  postgres:
    host: localhost
    port: 5432
    database: car_catalog

  api:
    car:
      http:
        client:
          host: https://example.api.car/info
          timeout: 3s